# Screenshot capture list

Use real PersonaFoil hardware captures only:

1. Main PersonaFoil interface
2. Settings → Identity with active identity/fingerprint
3. New Identity / saved persona list
4. Settings → System → Check Updates
5. Successful diagnostic export or verified update confirmation

A short GIF showing Native → New Identity → changed fingerprint → restart → same fingerprint is useful after persistence is actually validated.
